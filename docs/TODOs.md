# 当前项目 TODO

> 更新日期：2026-08-29。本文是项目唯一的当前 TODO 文档。
> 组织方式：按用户可感知的 Feature 管理；每个 Feature 同时包含后端、API、前端和验收。
> 当前范围：浏览器前端、FastAPI、application 和项目侧 service。桌面 exe、便携版、PyWebView 暂停；Gradio 保留为回退入口。

## 当前运行基线

- 浏览器开发命令：`kotaemon/.venv/Scripts/python.exe scripts/start_frontend_dev.py`。
- 前端：`http://127.0.0.1:3000`；FastAPI：`http://127.0.0.1:8000`。
- 正式 SQLite 已恢复并清理测试项目：仅保留“学习 Rust”项目（ID `8`，34 个节点）。
- 原损坏库保留在 `kotaemon/ktem_app_data/user_data/sql.db.corrupt-20260828.bak`。
- 已完成的迁移代码遵循 `Next.js -> FastAPI -> learning_ext/application -> learning_ext/service`；读取接口不隐式触发生成。

## 主 Feature List

> 当前执行顺序：Feature 1 -> Feature 2 -> Feature 3 -> Feature 5。Feature 4 和 Feature 6 暂时挂起，保留原编号以维持文档引用稳定。

### Feature 1：创建并调整学习路线，选择项目开始学习

#### 已完成

- [x] 项目列表、项目选择、路线读取、路线预览、路线调整和项目保存已接入 application、API 和 Next.js。
- [x] Gradio 路线页继续调用相同 application，用作回退和对照。
- [x] 将学习工作区改为功能标签页：按学习概览、学习路线、到期复习、RAG 问答、查漏测验、模型配置排序；RAG 问答合并资料库与资料问答。学习路线在节点列表与节点详情间分层切换，不在同一页面同时显示。
  - 实际修改：`frontend/app/page.tsx` 将顶栏收敛为六项；“学习路线”使用列表/详情两级状态，进入节点后只渲染 `LESSON DETAIL` 与“返回学习路线”，跨标签保留详情状态；RAG 问答中并列资料库和资料问答组件。`frontend/app/styles.css` 将标签栏改为六列并增加详情返回区；中等宽度下节点状态控件移至正文下方，避免挤压节点标题和说明。
  - 自动验证：前端 `npm run typecheck` 通过。
  - 手工验证：正式 Rust 项目顶栏顺序与文案符合要求；进入详情时 `ROADMAP` 列表不可见、`LESSON DETAIL` 与返回按钮可见，点击返回后列表恢复；RAG 问答内同时存在资料库和资料问答；切换到学习概览再返回学习路线仍保持当前详情；871px 窗口和 390px 窄屏均无横向溢出。
  - 遗留问题：无。
  - 完成日期：2026-08-28
- [x] 学习路线节点卡片整卡可点击进入详情，并提供明确的鼠标悬停和键盘焦点反馈；状态切换控件保持独立操作。
  - 实际修改：节点信息区改为语义化主操作按钮，移除“查看详情”文字按钮；状态控件移为同级独立操作。卡片增加悬停、焦点和过渡反馈，中等宽度与窄屏下状态控件位于信息区下方。
  - 自动验证：前端 `npm run typecheck` 通过；卡片主操作区为 `BUTTON`，无嵌套按钮。
  - 手工验证：悬停后卡片背景从白色变为浅绿、边框变绿；点击卡片进入 `LESSON DETAIL`，路线列表隐藏且返回按钮可用。
  - 遗留问题：无。
  - 完成日期：2026-08-28

#### 待完成

- [x] 真实 Rust 项目路线浏览器验收：阶段、节点依赖、空数据和正常路线操作。
  - 执行状态：已验证正式 Rust 项目 3 个阶段、34 个节点、依赖关系、节点详情进入/返回和状态切换；状态切换后已恢复原状态。使用独立临时数据目录验证 0 节点项目的 API 返回与前端空状态分支，未修改正式 Rust 学习数据。
  - 遗留问题：无。
  - 完成日期：2026-08-29

### Feature 2：打开节点详情，生成课程并完成学习

#### 已完成

- [x] 工作台聚合项目、进度、节点、课程、实操、笔记和资料摘要。
- [x] 节点详情读取、状态更新、课程生成、实操课程生成、资料生成、笔记保存已接入 application、API 和 Next.js。
- [x] 修复 `LESSON DETAIL` Markdown 渲染：标题、列表、代码块、表格、引用和链接正常显示，不渲染不安全 HTML。
  - 实际修改：新增 `frontend/features/markdown/MarkdownContent.tsx`，使用 `react-markdown` 和 `remark-gfm` 解析课程内容；启用 `skipHtml`，不接入原始 HTML 解析；详情页改为使用该组件，补充标题、列表、代码、引用、链接和表格样式。
  - 自动验证：前端 `npm run typecheck` 通过；`npm install` 审计 132 个包，发现 0 个漏洞。
  - 手工验证：正式 Rust 节点详情中检测到 27 个标题、14 个列表、28 个代码块、5 张表格和 4 段引用；原始 `##` 标题前缀不再出现，页面未发现 `script`、`iframe` 或 `style` 元素。
  - 遗留问题：无。
  - 完成日期：2026-08-28

#### 待完成

- [ ] 将已生成课程的“生成课程”按钮改为“重新生成课程”，避免用户误解当前节点尚未生成课程。
- [ ] 将节点详情拆为三个同层级、从上到下排列的页内窗口，使用中英双语标题：`LESSON DETAIL / 课程详情`、`PRACTICE LESSON / 实操课程`、`REFERENCE MATERIALS / 参考资料`。课程、实操和资料分别在对应窗口中展示与管理；点击“生成实操”和“拉取资料”后更新下方窗口，不跳转页面或遮挡课程 Markdown 内容。
- [ ] 真实 Rust 项目节点详情验收：课程切换、状态更新、课程/实操/资料生成和笔记保存。

### Feature 3：通过复习和测验巩固知识，查看进度并导出结果

#### 已完成

- [x] FSRS 到期卡片、复习评分、测验生成/答题、项目看板和路线/Markdown/HTML/Anki 导出已接入 Next.js。
- [x] 测验新题稳定关联 `node_id`，答题结果可以回流节点掌握度。

#### 待完成

- [ ] 将“知识点”选择框改为下拉菜单，验证空选项、长文本和移动端显示。
- [ ] 明确 HTML 报告“浏览器打印为 PDF”的产品提示；若需要 PDF 二进制，再单独实现并测试。
- [ ] 使用 Rust 项目真实数据完成复习、测验、看板和导出回归，覆盖空数据和正常提交。

### Feature 5：分别管理并验证 LLM 与 RAG 向量模型配置

#### 已完成

- [x] 单套模型配置的读取、保存、密钥脱敏和临时连通性测试已接入 API/Next.js。

#### 待完成

- [ ] 将模型配置拆分为两个独立配置区域：`LLM CONFIGURATION / LLM 配置` 负责对话、路线、课程和测验使用的大语言模型；`RAG EMBEDDING CONFIGURATION / RAG 向量模型配置` 负责资料索引和检索使用的向量模型。两者分别维护服务地址、模型名称、API Key、保存状态和连通性测试，修改或切换其中一类配置不得覆盖另一类配置。
- [ ] 支持本地单用户多个配置档案：新建、命名、编辑、切换、删除和连通性测试。
- [ ] 密钥不回填、不出现在日志/API 响应；保存 `kotaemon/.env` 前保留原配置并验证兼容性。
- [ ] 配置切换后分别验证 LLM 配置对对话、路线、课程和测验的生效范围，以及 RAG 向量模型配置对资料索引和检索的生效范围。

## 工程基础：数据、测试与文档

### 数据与工程保障

- [x] 正式数据库已由可验证隔离副本恢复；损坏原库已保全。
- [ ] 为正式 Rust 数据库补充备份、恢复和回退操作说明，并验证真实浏览器读写。
- [ ] 项目测试必须自然退出并记录真实退出码；仓库底座测试的导入问题单独记录。
- [ ] 补充 `docs/ARCHITECTURE.md`、README 和 `learning_ext/README.md` 的前端技术栈、目录职责、调用链、正式/隔离数据、日志和测试命令。

## 功能验收顺序

- [ ] 先完成 Feature 1/2 的 Rust 项目真实浏览器验收，再完成 Feature 3/5。
- [ ] 每个当前主 Feature 的后端、API、前端、业务流程和测试一起验收，不以单独接口通过作为完成。
- [ ] 所有日常流程完成真实验收、依赖扫描和回退确认前，不删除 Gradio Page 或启动链。

## 暂时挂起的 Feature

### Feature 4：上传学习资料，建立索引并进行带引用问答

> 状态：暂时挂起，不纳入当前主 Feature 验收顺序。

#### 已完成

- [x] Kotaemon RAG 已通过项目侧 adapter 接入，不修改 `kotaemon/` 底座。
- [x] 资料上传、索引进度、资料列表、精确确认删除、引用展示和流式回答已接入 API/Next.js。

#### 待完成

- [ ] 使用正式 Rust 数据目录验证资料上传、索引、选择范围、问答引用和删除；所有删除操作先显示影响范围。

### Feature 6：查询并管理 LLM 任务与异常处理

> 状态：暂时挂起，不纳入当前主 Feature 验收顺序。

#### 目标与范围

路线生成、审计、替换以及课程、资料、复习和测验等 LLM 操作统一进入可持久化、可查询、可恢复的任务生命周期，并集中处理超时、失败、取消、重试、日志和错误展示。任务可以在项目创建前的路线预览阶段产生，也可以关联已保存项目；本 Feature 只新增任务存储和相关接口/UI，不修改或删除现有项目、节点、资料和 Rust 学习数据。

#### 已完成

- [x] 批量内容准备已有任务状态、取消、重试和服务启动恢复。

#### 当前阻塞

- 执行状态：阻塞。
- 阻塞原因：生成和调整预览发生在项目创建前；现有 `le_task` 强制关联项目，不能可靠保存预览任务或支持重启恢复。
- 已尝试：复核并复用现有内容准备任务；它只适用于已保存项目，不能作为预览任务的事实来源。
- 需要决策：确认新增独立的持久化任务表（仅新增表，不修改或删除现有项目、节点、资料和 Rust 学习数据）。

#### 待完成

- [ ] 确认数据设计后，新增独立的持久化任务表，使项目创建前的预览任务和已保存项目任务都能可靠保存与恢复。
- [ ] 将路线生成、审计、替换、课程、实操、资料、复习和测验等 LLM 操作改为异步提交并返回任务 ID；提交、查询和执行过程保持请求、项目和节点范围可关联。
- [ ] 提供统一任务状态查询、取消和重试 API，并为成功、失败和取消定义明确终态。
- [ ] 约束重试和取消语义：重试生成可追踪的新执行记录；取消后进入明确终态，不再继续写入业务结果。
- [ ] 在路线预览、项目详情、资料和测验页面显示任务 ID、当前阶段、进度、失败原因、取消和重试操作；刷新页面或服务重启后可继续查询任务状态。
- [ ] 统一 LLM、向量模型、资料抓取和索引的超时边界、取消边界、有限重试与退避策略。
- [ ] 统一 logger、控制台和 API 错误结构并带上时间戳；日志关联请求、项目、任务和节点，但禁止输出密钥、完整提示词和敏感资料正文。
- [ ] 复核课程、资料和后台任务的 Session 生命周期，避免后台任务创建不可控 Session。
- [ ] 验证任务状态机、并发提交、重复操作、服务重启恢复、LLM 超时/异常和结构化错误事件；错误响应给出任务 ID 与下一步建议。
- [ ] 验证路线操作的日志能关联请求、项目、任务和节点，且不泄露密钥或完整提示词。
  - 执行状态：请求编号和项目/节点范围日志已实现；仍需对生成、审计、替换完整链路做最终验收。
  - 实际修改：FastAPI 为请求生成/透传 `X-Request-ID`，响应头和 `uvicorn.error` 控制台日志使用同一编号；路线预览、调整、保存、审计和替换记录项目范围与节点数量，不记录密钥或完整提示词；开发服务子进程固定 UTF-8 输出。
  - 自动验证：API 契约测试覆盖请求编号透传，application 测试覆盖路线预览日志上下文；相关后端测试 55 项通过。
  - 手工验证：实服务请求 `rust-route-cn-20260828` 已在响应头和 `logs/frontend-dev-api.log` 同时出现，中文“HTTP 请求完成”无乱码。
- [ ] 完成正式 Rust 项目路线生成、审计、替换的浏览器验收：覆盖预览任务、成功、失败、取消、重试、刷新和服务重启后的状态恢复。
- [ ] 完成课程、资料、复习和测验任务的跨页面异常与重试验收，确保失败任务可重试且不会静默丢失结果。

## 其他暂停范围

- [ ] 桌面 exe、便携版、PyWebView 和桌面端矩阵：用户已明确暂停，不阻塞浏览器前后端工作。
- [ ] Gradio 移除：等待所有 Feature 完成最终对照回归，并再次取得删除确认。

## 已知风险

- 真实 LLM、外网抓取、嵌入模型和不同文档格式仍可能失败，可靠性和错误可见性是后续重点。
- 当前正式库来自清理后的隔离副本，原损坏库中无法读取的其他历史数据没有恢复；备份仍保留。
